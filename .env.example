# Root Environment Variables
# Create a .env file locally - do NOT commit the real .env file.

# Backend DB (PostgreSQL - future)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=risktrace
DB_USER=postgres
DB_PASSWORD=secret

# AI API Key (future)
AI_API_KEY=your_api_key_here
